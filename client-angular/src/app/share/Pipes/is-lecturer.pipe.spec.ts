import { IsLecturerPipe } from './is-lecturer.pipe';

describe('IsLecturerPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLecturerPipe();
    expect(pipe).toBeTruthy();
  });
});
