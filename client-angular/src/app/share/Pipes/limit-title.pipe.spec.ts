import { LimitTitlePipe } from './limit-title.pipe';

describe('LimitTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
